table.table.table-striped
  thead
    tr
      th Name
      th Body
      th
      
  tbody
    - entries.each do |entry|
      tr
        td= link_to entry.name, entry
        td= entry.body.truncate(30)
        td
          = link_to "Edit", edit_entry_path(entry)
          '
          |/
          '
          = link_to "Destroy", entry, method: :delete, data: { confirm: "Are you sure?" }
